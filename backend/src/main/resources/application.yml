spring:
  application:
    name: backend
  profiles:
    active: docker
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
info:
  application:
    name: backend
    description: Backend service
    author: Peter Červeň
    version: 1.0.0
server:
  port: 8080
frontend:
  url: ${FRONTEND_URL}

