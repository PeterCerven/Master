spring:
  application:
    name: backend
  profiles:
    active: docker
  datasource:
    driver-class-name: org.postgresql.Driver
    username: user
    password: password
  sql:
    init:
      mode: always
server:
  port: 8080
